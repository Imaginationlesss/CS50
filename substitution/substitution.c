#include <cs50.h>
#include <ctype.h>
#include <string.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    //Check the key
    if(argv[])
    //Receive input
    string plaintext = get_string("plaintext: ");

    //Check the input
}